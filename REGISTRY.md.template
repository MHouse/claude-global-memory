<!-- Preamble managed by the claude-global-memory bootstrap (re-run with -Force / --force to resync).
     Rows under '## Registered hooks' are per-machine; edit freely. -->

# Hooks registry

This machine's `PreToolUse`/event hooks. **The claude-global-memory scaffold
installs no hooks and never edits `settings.json`** — this ledger only tracks
hooks you add yourself.

A hook is integration plumbing, governed like code, not a memory entry. Before
adding one it must clear the admission policy in
[`HOOKS.md`](https://github.com/MHouse/claude-global-memory/blob/main/HOOKS.md)
(all six points + a postmortem). Adding a hook is a 3-step manual recipe
(script + `settings.json` block + a row below); removing one reverses all three.
The full recipe lives in `HOOKS.md`.

## Registered hooks

| Hook | Event / matcher | Trigger | Injects | Owning memory file | Removal criteria |
|---|---|---|---|---|---|
